<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:theme="@style/Theme.LokalMarket">

        <activity
            android:name=".v1.ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.LokalMarket.Splash">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".v1.ui.auth.LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".v1.ui.auth.RegisterActivity"
            android:exported="false"/>

        <activity
            android:name=".v1.ui.main.MainActivity"
            android:exported="false" />

        <activity
            android:name=".v1.ui.main.DetailProdukActivity"
            android:exported="false"/>

        <activity
            android:name=".v1.ui.main.CheckoutActivity"
            android:exported="false"/>

        <activity
            android:name=".v1.ui.main.KeranjangFragment"
            android:exported="false"/>

    </application>

</manifest>